{{#each Users}}
<div class="card border-info mb-3" style="max-width: 18rem;">
    <div class="card-header">If this is your user then click button</div>
    <div class="card-body text-info">
       <button href='' class='btn btn-primary'>{{name}}</button>
    </div>
</div>

{{/each}}